{
  "name": ".exe-services",
  "productName": ".exe Services",
  "version": "1.0.0",
  "description": "A spoofer application.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "xvfb-run --auto-servernum electron-packager . --platform=win32 --arch=x64 --out=dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.3.2",
    "electron": "^28.0.0"
  }
}
